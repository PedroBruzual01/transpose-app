org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Pin the JBR bundled with Android Studio (JDK 21) so the build doesn't pick up
# whatever JDK happens to be first on PATH (this machine also has JDK 26, which
# current AGP/Gradle versions do not support running on).
org.gradle.java.home=C:/Program Files/Android/Android Studio/jbr
